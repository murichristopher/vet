<%= form_with(model: hospitalization_item, local: true, html: { class: "needs-validation", novalidate: "" }) do |form| %>
  <% if hospitalization_item.errors.any? %>
    <div class="alert alert-danger" role="alert">
      <h4 class="alert-heading">Foram encontrados <%= pluralize(hospitalization_item.errors.count, "erro") %>:</h4>
      <ul>
        <% hospitalization_item.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :pet_id, "ID do Pet", class: "form-label" %>
    <%= form.number_field :pet_id, class: "form-control", required: true %>
    <div class="invalid-feedback">
      Por favor, informe o ID do pet.
    </div>
  </div>

  <div class="mb-3">
    <%= form.label :status, "Status", class: "form-label" %>
    <%= form.text_field :status, class: "form-control", required: true %>
    <div class="invalid-feedback">
      Por favor, informe o status da internação.
    </div>
  </div>

  <div class="mb-3">
    <%= form.label :name, "Nome", class: "form-label" %>
    <%= form.text_field :name, class: "form-control", required: true %>
    <div class="invalid-feedback">
      Por favor, informe o nome do item de internação.
    </div>
  </div>

  <div class="actions">
    <%= form.submit "Salvar", class: "btn btn-success" %>
  </div>
<% end %>

<script>
// Example starter JavaScript for disabling form submissions if there are invalid fields
(function() {
  'use strict';

  window.addEventListener('load', function() {
    >
<script>
// Example starter JavaScript for disabling form submissions if there are invalid fields
(function() {
  'use strict';

  window.addEventListener('load', function() {
    // Fetch all the forms we want to apply custom Bootstrap validation styles to
    var forms = document.getElementsByClassName('needs-validation');

    // Loop over them and prevent submission
    var validation = Array.prototype.filter.call(forms, function(form) {
      form.addEventListener('submit', function(event) {
        if (form.checkValidity() === false) {
          event.preventDefault();
          event.stopPropagation();
        }
        form.classList.add('was-validated');
      }, false);
    });
  }, false);
})();
</script>